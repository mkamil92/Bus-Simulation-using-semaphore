First Extract the folder "OS_Project" and then open "OS_Project".
Now follow the following steps:

	=> Double Click the "Project.jar" it is the complete project.
	=> If you want check only source code then open the folder "Source Code" and double click on "RunGUI.java".
	=> If you want to compile and run the project then open the project folder "OS Project" in any IDE i.e Eclipse of Netbean.

		RunProject.java(In "Source Code" folder)
	=> RunProject.java is just the code based project mean it is just for printing the values and waiting.

---------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------

"UseCase.PNG" is the Usecase of the Project...
	   
======================================================================================================



